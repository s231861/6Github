# Headline

x <- rnorm(100)
y <- rnorm(100)

# Important feature


# Competition
library(tidyverse)
a <- tibble(name =c("a", "b", "c"), id = c("d","e","f"))
# USe sample afterwards sample(a$name, size = 1)

# New feature
