# Headline

x <- rnorm(500)
y <- rnorm(500)


# Important feature


# Competition
library(tidyverse)
a <- tibble(name =c("a", "b", "c"), id = c("d","e","f"))
# USe sample afterwards sample(a$name, size = 1)

# Normal
